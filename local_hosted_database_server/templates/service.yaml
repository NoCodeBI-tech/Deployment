{{- range $service := .Values.services }}
apiVersion: v1
kind: Service
metadata:
  name: {{ $.Values.global.appName }}-{{ $service.name }}-{{ substr 0 10 $.Values.global.env.INSTANCE_ID | lower }}-service
  namespace: {{ $.Values.global.appName }}
spec:
  selector:
    app: {{ $.Values.global.appName }}-{{ $service.name }}-{{ substr 0 10 $.Values.global.env.INSTANCE_ID | lower }}
  ports:
  - protocol: TCP
    port: 80
    targetPort: {{ $service.port }}
---
{{- end }}